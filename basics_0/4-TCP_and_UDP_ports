#!/usr/bin/env bash

# This script displays listening TCP, UDP, and UNIX domain sockets,
# along with the PID and name of the corresponding program.

echo "Active Internet connections (only servers)"
# The 'netstat -ltupn' command lists all TCP and UDP listening ports
# and their associated PIDs and program names.
# -l: Show only listening sockets
# -t: Show TCP sockets
# -u: Show UDP sockets
# -p: Show PID and program name
# -n: Show numerical addresses and port numbers
netstat -ltupn

echo ""
echo "Active UNIX domain sockets (only servers)"
# The 'netstat -lxp' command lists all listening UNIX domain sockets
# and their associated PIDs and program names.
# -l: Show only listening sockets
# -x: Show UNIX domain sockets
# -p: Show PID and program name
netstat -lxp
